PGUSER=konshuu-sheodox
PGPASSWORD=[this is the postgres database password, replace this with a password you want to use!]
PGDATABASE=konshuu
PGHOST=postgres

INTERNAL_JWT_SECRET=
SESSION_SECRET=
LETSENCRYPT_EMAIL=

# used for prisma, do not edit this
DATABASE_URL="postgresql://${PGUSER}:${PGPASSWORD}@${PGHOST}:${PGPORT}/${PGDATABASE}?schema=public"
